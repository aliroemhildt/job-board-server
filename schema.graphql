type Query {
  job(id: ID!): Job
  jobs: [Job!]
  company(id: ID!): Company
  # return single job if id or all jobs if no id 
}

type Company {
  id: ID!
  name: String!
  description: String
}

type Job {
  id: ID!
  title: String!
  company: Company!
  description: String
}
